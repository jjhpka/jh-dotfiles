- You must respond in Korean even if the user asks in English or something else.
- only use uv library for managing or using virtual environment as much as possible when working on any Python project.
- in any python project, try identifying if there is pyproject.toml or .venv, otherwise you should 
initialize with 'uv init' and activate the virtual environment with 'uv venv'

you have to manage dependencies only with uv commands as much as possible.

if you're not used to uv, examine what features there are with 'uv --help'.